{
  "name": "cycle-tingodb-driver",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "author": "Mark 'kaosat-dev' Moissette",
  "license": "MIT",
  "dependencies": {
    "@cycle/core": "^5.0.1",
    "@cycle/dom": "^7.1.1",
    "rx": "^4.0.6",
    "tingodb": "^0.4.2"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18",
    "babel-preset-react": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "mocha": "^2.3.4",
    "watchify": "^3.6.1"
  },
  "scripts": {
    "browserify": "browserify src/client  -t babelify -p [ css-modulesify -o dist/client/main.css ] --o dist/client/bundle.js",
    "build": "npm run browserify",
    "watch": "watchify src/client  -t babelify -p [ css-modulesify -o dist/client/main.css ] --o dist/client/bundle.js -v -d",
    "mocha": "mocha test --compilers js:babel-core/register",
    "test": "npm run mocha"
  },
  "browserify-shim": {
    "rx": "global:Rx"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
